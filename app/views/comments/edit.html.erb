<div class='container',style="margin-top: 10rem; margin-left: 25rem">
<%= form_for ([@grade, @comment]) do |f| %>
    <%= f.text_area :content, class:"single-input"%>
    <%= f.submit "grade comment", class:"genric-btn info-border circle" %>
      <%end%>
<div>
