<ul>
  <% comments.each do |comment| %>
    <% if comment.id.present? %>
      <li>
        <p><%= comment.content %></p>
        <p><%= link_to 'Delete', [comment.grade, comment], :method => :delete, data: { confirm: 'Are you sure?' }%></p>
         <p> <%=link_to 'Edit', edit_grade_comment_path(@grade, @comment)%>
        </li>
    <% end %>
  <% end %>
</ul>