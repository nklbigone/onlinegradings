<ul>
  <% @comments.each do |comment| %>
      <li>
        <p><br><%= comment.content %><br>
        <%= link_to "delete", [comment.grade, comment], method: :delete, class:"genric-btn danger-border circle" %>
        <%= link_to "edit ", edit_grade_comment_path(comment.grade, comment), class:"genric-btn success-border circle" %></p>
  <% end %>
<%= render 'comments/form'%>
</ul>